{
  "name": "media-share-platform",
  "version": "1.0.0",
  "description": "媒体分享平台",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
}
